# Lexis — Güncel Kalan İşler Listesi

*(21 Ağustos 2026, on üçüncü güncelleme (en güncel) — **React Native mobil uygulama, Faz 1 kapsamıyla sıfırdan inşa edildi, kullanıcının bilgisayarına teslim edildi ve gerçek bir Android tablete kurulup çalıştığı doğrulandı.** Tech stack seçimi tamamen asistana bırakıldı ("hiç mobil uygulama geliştirmedim"): Expo (SDK 57, managed workflow) + TypeScript + expo-router, Expo Push Notifications, iOS+Android birlikte. Kapsam: auth+OTP, dashboard (XP/streak/leaderboard önizleme), kelimeler (listele/ara/sözlükten ekle/sil), oyun (çoktan seçmeli + wordle, 3 yön — challenge modu mobile henüz yok), program (schedule: listele/ekle/aç-kapa/sil), profil (dil yönetimi/çıkış), push bildirim **altyapısı** (izin isteme + token kaydı; gerçek bildirim gönderimi Faz 2/3'e bırakıldı). Web'in `lib/i18n.tsx`'indeki 9 dil × 274 anahtarlık merkezi çeviri sözlüğü elle yeniden yazılmadan programatik olarak (Python brace-matching parser) aktarıldı. Backend'e yeni `push_tokens` tablosu (migration `017_push_tokens.sql`, canlıya uygulandı, RLS'li) ve `/api/v1/me/push-tokens` route'u eklendi, `main.py`'a bağlandı — **kullanıcının backend'i yeniden başlatması gerekiyordu, yapıldı.** `npx tsc --noEmit` 0 hata, `npx expo export` başarılı bundle ile doğrulandı. 62 dosyalık `mobile/` projesi zip'lenip kullanıcının bilgisayarına teslim edildi, `npm install` + `npx expo start` ile Expo Go üzerinden test edildi. Ardından kullanıcı "gerçek bir uygulama" beklediğini belirtince (Expo Go'nun geliştirme modu olduğu anlaşılamamıştı) **EAS Build ile bağımsız bir Android APK üretildi** (`eas.json` eklendi, kullanıcı kendi ücretsiz Expo hesabıyla `eas-cli login` + `eas build --platform android --profile preview` çalıştırdı, ~38 dk sürdü — ücretsiz plan kuyruk süresi normal), Google Play Protect'in imzasız APK uyarısı aşılıp **tablete kuruldu ve açıldığı doğrulandı.** **Kullanıcının yapması/karar vermesi gerekenler:** iOS için gerçek cihaza kurulum ya da TestFlight, Apple Developer Program üyeliği (yıllık ~99$) gerektiriyor — henüz alınmadı, mobil App Store yayını da aynı hesaba bağlı. `mobile/`de `npx expo lint` bu ortamda ağ kısıtlaması yüzünden hiç çalıştırılamadı (kullanıcının kendi makinesinde denemesi önerilir). EAS build log'unda zararsız görünen ama incelenmemiş bir "Run expo doctor" uyarısı var. **Sıradaki adım:** kullanıcı kararına göre ya mobil Faz 2/3/4'e (sosyal özellikler, Premium/WebView, mağaza yayın hazırlığı) devam edilecek ya da doğrudan Madde 5'e dönülüp kurumsal siteye mobil uygulama görselleri/QR/linkleri eklenecek.)*

*(21 Ağustos 2026, on ikinci güncelleme (önceki) — **Kullanıcı `landing/` projesini Vercel'e deploy etti (GitHub — `abtaylan/lexis` reposu — üzerinden import edilip canlıya alındı) ve ekran görüntüsüyle sonucu paylaştı: sayfa gerçek ekran görüntüleriyle beklendiği gibi görünüyor.** Bu vesileyle netleşen kararlar ve yapılan ek değişiklik: **(1) Bilinen sınırlama not edildi:** arayüz dili değiştirilse bile ekran görüntüleri (`dashboard.png`/`words.png`/`game.png`) hep aynı Türkçe içerikli görsel olarak kalıyor — her dil için kendi ekran görüntüsünün alınması gerekiyor, henüz yapılmadı (aşağıda Madde 5'e madde olarak eklendi). **(2) Domain alımı kullanıcı kararıyla ertelendi** — mobil uygulama da tamamlanınca birlikte yapılacak, şimdilik Vercel'in verdiği geçici `*.vercel.app` adresiyle devam ediliyor. **(3) Ortam değişkenleri kullanıcı kararıyla Vercel'e henüz girilmedi** — iletişim e-postası/Telegram-Slack-LinkedIn linkleri/production `NEXT_PUBLIC_APP_URL` netleşip canlıya alım süreci tamamlanınca hepsi birlikte Vercel proje ayarlarına girilecek (`landing/README.md`'ye bu karar not düşüldü). **(4) Sosyal medya listesine LinkedIn eklendi (kod tarafı bitti):** `src/lib/config.ts`'teki `SocialLink.key` union'ına `'linkedin'` eklendi, `SOCIAL_LINKS`'e `NEXT_PUBLIC_LINKEDIN_URL` okuyan yeni bir girdi eklendi (hesap henüz açılmadığı için diğerleri gibi `href: null` — "yakında"), `icons.tsx`'e lucide-react'te bulunmayan bir `LinkedinLogoIcon` (özel SVG, YouTube/Instagram/X/Slack ile aynı desen) eklendi, `Footer.tsx`'teki `ICONS` eşlemesine bağlandı. `npx eslint`/`npx tsc --noEmit`/`npx next build` temiz geçti, Playwright ile footer'da yeni "in" ikonunun diğerleriyle tutarlı (gri, tıklanamaz, "yakında" tooltip'li) göründüğü doğrulandı. **(5) Yeni bir gereksinim not edildi:** kurumsal siteye mobil uygulama bilgileri de eklenmeli — App Store/Google Play rozetleri, bir karekod (QR) ve erişim linkleri; bu iş mobil uygulama (Faz 3) tamamlanıp yayınlandıktan sonra yapılabilir çünkü şu an bağlanacak gerçek bir store linki yok (aşağıda "Büyük, henüz başlanmamış işler" ve Madde 5'e not edildi). **(6) "Diğer platform hesapları" listesi güncellendi:** kurumsal site için netleşen nihai liste (YouTube, Instagram, X, Telegram ✅, Slack ✅, LinkedIn — WhatsApp kullanıcı kararıyla dahil değil) ile önceki, daha eski bir listedeki WhatsApp Business/Facebook referansları arasındaki tutarsızlık bu güncellemeyle giderildi (Facebook listeden çıkarıldı, WhatsApp zaten hariçti, LinkedIn eklendi). 4 dosya (`icons.tsx`, `config.ts`, `Footer.tsx`, `README.md`) kullanıcının bilgisayarına yazıldı. **Kullanıcının yapması gerekenler:** hiçbiri acil değil — env değişkenlerini ve domain'i kendi belirlediği zamanlamayla (mobil uygulama bitince) ekleyecek. **Sıradaki adım:** React Native mobil uygulamasına başlamak.)*

*(21 Ağustos 2026, on birinci güncelleme (önceki) — **Madde 5, gerçek ekran görüntüleri alındı ve landing sayfasına entegre edildi — Madde 5 artık tamamen bitti (sadece kullanıcının `.env.local` değerlerini girmesi kaldı).** Kullanıcı yerel `web`+backend dev server'larına giriş yaptıktan sonra ("şuan giriş yaptım, resimleri alabilirsin") Claude in Chrome ile kullanıcının kendi bilgisayarındaki tarayıcıya bağlanılıp 3 gerçek ekran görüntüsü alındı: **Dashboard** (günaydın karşılama + XP/seviye çubuğu + sıralama bölümü), **Kelimeler** (test hesabına önceden sadece 1 kelime vardı, ekranın boş görünmemesi için uygulamanın kendi "Kelime Ekle" akışıyla 4 kelime daha eklendi — toplam 5: curiosity/resilient/eloquent/serendipity/resilience), **Kelime Oyunu** (mod → yön → havuz seçimi adımları tek tek geçilip gerçek bir soru ekranına ulaşıldı, bir seçenek işaretlenmiş haliyle yakalandı). Ham ekran görüntüleri (958×888) Python/Pillow ile işlendi: **Dashboard'daki sıralama bölümü kasıtlı olarak sadece en üstteki "testlexis (Sen) — 1." satırına kadar kırpıldı** — altındaki gerçek test hesap adları (`bugtest2026`, `Bug Test Uc`, `ytt` gibi) kurumsal bir tanıtım sayfasında görünmesi uygun olmadığı için dışarıda bırakıldı, bu sayede ayrıca ekran "kullanıcı bir numarada" gibi olumlu bir görüntü de veriyor. Kelimeler ve Oyun ekranları da alttaki boşluk kırpılarak aynı orana (900×577, ~1.56:1 — yatay/masaüstü oranı) getirildi. `landing/public/screenshots/{dashboard,words,game}.png` yer tutucuların üzerine yazıldı; `Hero.tsx` ve `Showcase.tsx`'teki `<Image>` `width`/`height` değerleri eski dikey telefon oranından (640×800 / 480×600) yeni yatay masaüstü oranına (900×577) güncellendi, Hero'daki çerçeve genişliği de (`max-w-sm` → `max-w-xl`) yeni yatay görsele göre büyütüldü. `npx eslint`/`npx tsc --noEmit`/`npx next build` tekrar temiz geçti, ayrıca `next build` çıktısı yerel olarak `next start` ile ayağa kaldırılıp Playwright'la Hero ve "Uygulamadan görüntüler" bölümleri görsel olarak doğrulandı — üç görsel de tutarlı görünüyor, sıralama bölümünde sorunlu isim görünmüyor. Güncellenen 5 dosya (3 png + 2 tsx) kullanıcının bilgisayarına yazıldı. **Kullanıcının yapması/karar vermesi gereken tek şey kaldı:** gerçek iletişim e-postası, Telegram/Slack linkleri, production `NEXT_PUBLIC_APP_URL` — `landing/README.md`'deki tabloya göre `.env.local`'e girilmeli. **Sıradaki adım:** kullanıcının onayladığı sırayla artık mobil uygulamaya (React Native) geçilecek.)*

*(21 Ağustos 2026, onuncu güncelleme (önceki) — **Madde 5, kurumsal tanıtım sayfası iskeleti kuruldu (`landing/` klasörü, `web/`den bağımsız ayrı Next.js projesi).** Kullanıcı onayıyla kapsam: repo içi ayrı klasör, kapsamlı/detaylı içerik (sadece tek sayfa değil), baştan 9 dilin hepsi, sosyal ikonlardan hazır olmayanlar (YouTube/Instagram/X) placeholder ile "yakında" gösteriliyor — Telegram/Slack zaten kurulu olduğu için linkleri ortam değişkeninden okunuyor (`NEXT_PUBLIC_TELEGRAM_URL`/`NEXT_PUBLIC_SLACK_URL`, henüz boş). Sayfa yapısı: Header (dil seçici + giriş/kayıt), Hero, Özellikler (6 kart), Uygulamadan görüntüler (3'lü, şimdilik yer tutucu görsel), Nasıl Çalışır (4 adım), SSS (6 soru, akordeon), CTA banner, Footer (ürün/şirket linkleri, sosyal ikonlar, e-posta — `NEXT_PUBLIC_CONTACT_EMAIL`, şimdilik yer tutucu `info@lexis.app`). Giriş/Kayıt butonları `NEXT_PUBLIC_APP_URL` (varsayılan `localhost:3000`) üzerinden `web/`e yönleniyor — **production'a alınırken bu değişkenin gerçek alan adına ayarlanması gerekiyor.** i18n mimarisi `web/src/lib/i18n.tsx` ile aynı desen (flat dictionary + Context), 9 dilin hepsi (tr/en/de/fr/es/it/ru/ar/ja) yazıldı, `ar` için `dir="rtl"` otomatik uygulanıyor ve görsel olarak doğrulandı (Playwright ile ekran görüntüsü alınıp kontrol edildi — nav/metin/buton hizalaması doğru mirrorlanıyor). Mobil görünüm de Playwright ile doğrulandı (hamburger menü, tek sütun düzen). Marka renkleri ve font (Outfit, sky-blue + mor vurgu) `web/`deki `globals.css`'ten alındı, logo dosyaları (`logo-icon.png`/`logo-full.png`) `web/public`'ten kopyalandı. `npx eslint`/`npx tsc --noEmit`/`npx next build` üçü de temiz geçti (tek eslint hatası — yine `react-hooks/set-state-in-effect`, i18n'in mount-time dil tespiti — gerekçeli yorumla işaretlendi, web/ deki 9. güncellemedeki çözümle aynı desen). 30 dosya `landing/` altına kullanıcının bilgisayarına yazıldı. **Ekran görüntüleri henüz gerçek değil** (`public/screenshots/dashboard.png` vb. — mavi çerçeveli yer tutucu), bunun için `web` (+ backend) dev server'ının çalışır olması ve bir hesapla giriş yapılmış olması gerekiyor — bir sonraki oturumda ele alınacak. **Kullanıcının yapması/karar vermesi gerekenler:** gerçek iletişim e-postası, Telegram/Slack linkleri (`.env.local`), production alan adı (`NEXT_PUBLIC_APP_URL`) — hepsi netleşince `landing/README.md`'deki tabloya göre `.env.local`'e eklenmeli. **Sıradaki adım:** gerçek ekran görüntülerini almak (kullanıcının dev server'ları çalıştırması gerekiyor), sonra Madde 5 tamamlanmış sayılacak ve mobil uygulamaya (React Native) geçilecek.)*

*(21 Ağustos 2026, dokuzuncu güncelleme — **eslint hataları tamamen temizlendi, `next build` artık geçiyor.** Kullanıcı "acil/öncelikli işler → kurumsal web sayfası → mobil uygulama → sosyal medya hesapları" sırasını onayladı, ilk adım olarak eslint temizliğini seçti. Proje klasörü masaüstü uygulaması üzerinden bağlandı, `web/src` altındaki tüm dosyalar + `package.json`/`package-lock.json` alınıp ayrı bir ortamda `npm ci` ile kurulup gerçek `npx eslint src` çalıştırıldı — **önceki notta belirtilen 45 hata artık 52 hataya, 20 dosya 27 dosyaya çıkmıştı** (Madde 6 Faz 2/3 sırasında eklenen `friends`/`messages`/`u/[username]`/`Leaderboard` dosyaları aynı `useEffect(load, [deps])` desenini taşıyordu). Dağılım ve çözüm: **`react-hooks/set-state-in-effect` (22 hata)** — hepsi zararsız "mount/parametre değişiminde veri çekme" deseni (React Compiler'ın yeni, agresif kuralı); davranış değiştirmeden, gerekçeli `eslint-disable-next-line` yorumuyla işaretlendi. **`@typescript-eslint/no-explicit-any` (16 hata)** — gerçek tiplerle değiştirildi: çoğu `catch (err: any)` → mevcut `getErrorMessage()`/`errorDetail()` yardımcılarına (zaten `lib/errors.ts`'te ve `profile/page.tsx`'te vardı, tutarlılık için diğer dosyalara da uygulandı) veya somut interface'lere (`WordPoolCreate`, `NewUserForm`, yeni `CronRunInfo`) bağlandı. **`react/no-unescaped-entities` (13 hata)** — JSX'teki kaçırılmamış `'`/`"` karakterleri `&apos;`/`&quot;` ile değiştirildi. **`react-hooks/immutability` (1 hata)** — `premium/page.tsx`'te `window.location.href = ...` → `window.location.assign(...)` (React Compiler mutasyon kontrolü property-assignment yerine metod çağrısını kabul ediyor). **`@next/next/no-location-assign-relative-destination` (1 hata)** — `lib/api.ts`'teki axios 401-interceptor yönlendirmesi (React ağacı dışında, kasıtlı tam sayfa yenilemesi) gerekçeli yorumla işaretlendi. Ayrıca **5 kullanılmayan değişken** (`LOCALE_MAP`, `DATE_LOCALE` ×2, `DAY_KEYS`, `TEMPLATE_ICONS` — hepsi gerçekten ölü kod, terk edilmiş eski i18n deseninden kalıntı) ve **2 bayat `eslint-disable` yorumu** temizlendi. Sonuç doğrulandı: `npx eslint src` → **0 hata, 0 uyarı**; `npx tsc --noEmit` → **0 hata**; `npx next build` → **başarılı, 30 route** (7 admin + 14 app + 6 auth + kök/not-found sayfaları, `messages/[username]` ve `u/[username]` dinamik). Değişen 27 dosya kullanıcının bilgisayarına (orijinal proje klasörüne) geri yazıldı. **Kullanıcının yapması gerekenler:** `web` dev server'ı yeniden başlat/yenile; fırsat bulunca `git diff` ile göz gezdirilmesi faydalı olur (özellikle `react-hooks/set-state-in-effect` disable'ları — hepsi mevcut davranışı korumak için bilinçli tercih, ileride bu sayfalardan biri gerçekten yeniden yazılırsa React'ın önerdiği desene (loading state'i `useState(true)` ile başlatıp hiç senkron `setTrue` çağırmama) geçilebilir ama bu kapsamda zorunlu değildi). **Sıradaki adım (kullanıcı onayıyla):** Madde 5 — kurumsal tanıtım web sayfası.)*

*(21 Ağustos 2026, sekizinci güncelleme (önceki) — **Madde 6 TAMAMLANDI: Faz 2 (mesajlaşma + engelleme) ve Faz 3 (meydan okuma) tek oturumda birlikte uygulandı.** Kullanıcı iki fazı birlikte istedi ("İkisini birlikte tek oturumda yap"). Migration `016_social_messaging_and_challenges.sql` (`blocks`, `conversations`, `messages`, `challenges` tabloları — hepsi RLS: sadece ilgili taraflar SELECT edebiliyor, yazma backend service-role client üzerinden) Supabase MCP ile **canlıya uygulandı**. Backend: `backend/app/services/{block_service,messaging_service,challenge_service}.py` yeni eklendi, mevcut `friends_service.py`/`follow_service.py`/`public_profile_service.py` engelleme kontrolü (`is_blocked_either_way`) ile genişletildi (engellenen kullanıcı arkadaşlık isteği/takip/mesaj/profil görüntüleme yapamıyor — karşılıklı kontrol edilir), `api/routes/social.py`'ye `/social/block/*`, `/social/blocked`, `/social/conversations/*`, `/social/messages/unread-count`, `/social/challenges/*` uçları eklendi. **Mesajlaşma:** gerçek zamanlı değil, polling tabanlı (bu kod tabanında hiç Realtime emsali yok — kasıtlı tercih). **Meydan okuma:** yeni bir oyun akışı YOK — mevcut `game_sessions` tablosu kullanılıyor, her taraf kendi bitmiş oturumunu `submit-score` ile gönderiyor, backend skorları karşılaştırıp kazananı belirliyor (`game/page.tsx` `?challengeId=&mode=` query param'larıyla açılınca mod seçimi atlanıp otomatik gönderim yapıyor). Frontend: `friends/page.tsx`'e 4. sekme ("Meydan Okumalar", `?tab=challenges` ile doğrudan açılabiliyor), `u/[username]/page.tsx`'e Mesaj/Engelle/Meydan oku butonları, `profile/page.tsx`'e "Engellenenler" yönetim bölümü, yeni `messages/page.tsx` (gelen kutusu) + `messages/[username]/page.tsx` (sohbet, polling ~4sn) sayfaları, Dashboard'a gelen kutusu önizleme kartı + çalışma dili seçicisinin yanına okunmamış mesaj rozeti/simgesi (kullanıcının açık isteği: "1 ve 2 birlikte olabilir" — hem rozet hem tıklanabilir ikon) eklendi. Hepsi 9 dilin tamamı için yerel çeviri ile. **Ayrıca bu oturumda, kullanıcının Faz 1 sonrası bildirdiği bir eksik giderildi:** Leaderboard'daki kullanıcı adları artık `u/[username]` profiline tıklanabiliyor (önceden statik metindi). **Ve bekleyen bir madde kapandı:** `seed_general_word_pool.py ru/ar/ja` kota açılınca kullanıcı tarafından çalıştırıldı — ru: 262 kelime eklendi, ar: 0 eklendi (287 zaten mevcuttu), ja: 0 eklendi (255 zaten mevcuttu) — `general_word_pool` artık 8/8 dilde dolu. Python `pyflakes`, TS/TSX `esbuild` ile denetlendi, hepsi temiz — **kullanıcının `npx tsc --noEmit` ve `npx eslint src`'i de çalıştırması önerilir.** **Kullanıcının yapması gerekenler:** backend ve web dev server'ı yeniden başlat/yenile (yeni route'lar/sayfalar için).)*

*(21 Ağustos 2026 güncellemesi — Madde 1d admin panel tamamen kapandı; genel kelime havuzu, i18n/Premium çevirisi, döviz bazlı fiyatlandırma altyapısı ve birkaç teknik borç maddesi bu oturumlarda çözüldü. Bu dosya `lexis_sosyal_medya_hesap_hazirlik.md` ile aynı yerde, repo kökünde tutuluyor.)*

*(21 Ağustos 2026, ikinci güncelleme — `npm audit fix` uygulandı (0 açık); XPBar bileşeni oluşturulup Sidebar/Dashboard'a bağlandı; döviz bazlı Premium'un iyzico tarafının **henüz yapılmadığı** doğrulandı (`.env`'de sadece placeholder var); tsc taraması sırasında ilgisiz ama gerçek birkaç derleme hatası bulundu.)*

*(21 Ağustos 2026, üçüncü güncelleme — Logo seçildi (18.13.05) ve web'e (login + Sidebar/Dashboard + favicon/app icon) bağlandı; mobil ve kurumsal site için marka varlıkları hazırlandı (henüz o projeler yok); iyzico 6 plan oluşturma script'i yazıldı (`backend/setup_iyzico_plans.py`, henüz **çalıştırılmadı**); bir önceki güncellemede bulunan tsc/build hataflarının tamamı düzeltildi; bu düzeltmeleri doğrularken **ayrı ve daha büyük bir sorun** bulundu — eslint 20 dosyada 45 hata veriyor, `next build` muhtemelen bu yüzden de başarısız oluyor.)*

*(21 Ağustos 2026, dördüncü güncelleme — **Kullanıcı geri bildirimiyle logo 18.10.47'ye değiştirildi** (18.13.05 önceki öneriydi, kullanıcı "çok güzel gözükmedi" dedi). Tüm web/mobil/docs varlıkları 18.10.47'nin maskot karakterinden yeniden üretilip aynı dosya yollarına yazıldı. İşlem sırasında ilk kırpmada komşu bir konuşma balonundan (mor "Bonjour!" balonu) sarkan bir piksel sızıntısı fark edilip renk bazlı maskeleme ile temizlendi. Ayrıca kullanıcının bildirdiği `python setup_iyzico_plans.py` → `401 Unauthorized` hatası kök nedeniyle birlikte teşhis edilip `iyzico_client.py`'de düzeltildi (JSON imza/gövde serileştirme uyuşmazlığı — aşağıda ayrıntılı).)*

*(21 Ağustos 2026, beşinci güncelleme — Logo web'de eski görünmeye devam etti: dosyalar diskte doğruydu, sorun Next.js dev server'ın `next/image` optimizasyon cache'iydi (`.next/cache/images`) — kullanıcı `.next` klasörünü silip dev server'ı yeniden başlatınca düzeldi. **iyzico 401 hatası ikinci denemede de devam etti** — önceki gövde-serileştirme düzeltmesi tek başına yeterli değildi. `.env`'deki API key/secret formatı kontrol edildi (sandbox öneki yok, tırnak/boşluk sorunu yok — temiz görünüyor), ama gerçek sebep hâlâ belirsiz çünkü `raise_for_status()` iyzico'nun asıl hata gövdesini (`errorCode`/`errorMessage`) gizliyordu. `iyzico_client.py` ve `setup_iyzico_plans.py` artık hata durumunda iyzico'nun tam yanıt gövdesini yazdırıyor — **kullanıcının script'i tekrar çalıştırıp çıkan "Yanıt gövdesi:" satırını paylaşması bekleniyor**, kesin teşhis ancak o zaman konulabilir.)*

*(21 Ağustos 2026, yedinci güncelleme — **Madde 6, Faz 1 uygulandı: Arkadaşlık sistemi + Takip etme + Başkasının profilini görüntüleme.** Faz bölünmesi kullanıcı tarafından asistana bırakıldı, önceki oturumun kendi önerisi olan 3 faz (Faz 1: arkadaşlık+takip+profil / Faz 2: mesajlaşma+engelleme / Faz 3: meydan okuma) uygulandı. Profil görüntüleme gizliliği kullanıcı kararıyla **herkese açık** (giriş yapmış her kullanıcı başka birinin istatistik özetini ve çalışma programını görebilir, ek izin kontrolü yok). Migration `015_social_friends.sql` (`friendships` + `follows` tabloları) Supabase MCP ile **canlıya uygulandı** (`apply_migration`, migration adı `social_friends_and_follows`) — kullanıcının elle SQL çalıştırmasına gerek yok. Backend: `backend/app/schemas/social.py`, `backend/app/services/{friends_service,follow_service,public_profile_service}.py`, `backend/app/api/routes/social.py` (`/api/v1/social/*`), `main.py`'a router kaydı eklendi. Frontend: `web/src/app/(app)/friends/page.tsx` (3 sekme: Arkadaşlarım / İstekler / Kullanıcı Ara) ve `web/src/app/(app)/u/[username]/page.tsx` (herkese açık profil: istatistik + çalışma programı + arkadaşlık/takip aksiyonları) yeni sayfalar olarak eklendi, Sidebar'a "Arkadaşlar" linki bağlandı, `api.ts`/`types/index.ts` güncellendi — merkezi `i18n.tsx`'e dokunulmadan (XPBar/Leaderboard'daki güvenli desenle) 9 dilin hepsi için yerel çeviri eklendi. Bildirimler mevcut `notifications` altyapısına entegre edildi (yeni istek/kabul bildirimleri, yeni tablo gerekmedi). Python dosyaları `pyflakes` ile, TS/TSX dosyaları `esbuild` ile sözdizimi/kullanılmayan-import denetiminden geçirildi (bu oturumda kullanılabilecek `npx tsc`/`npx eslint` çalıştırma imkanı yoktu — **kullanıcının bir sonraki `npx tsc --noEmit` ve `npx eslint src` çalıştırmasında Faz 1 dosyalarını da kontrol etmesi önerilir**, özellikle mevcut 45 eslint hatasının üstüne yeni hata eklenmediğinden emin olmak için `catch (err: any)` yerine bilinçli olarak `AxiosError` tip kontrolü kullanıldı). **Kullanıcının yapması gerekenler:** backend'i yeniden başlat (yeni route'lar için) ve `web` dev server'ı yeniden başlat/yenile (yeni sayfalar için); `.next` cache sorunu daha önce yaşandığı için sayfa eskisi gibi görünürse `.next` silinip dev server yeniden başlatılmalı (bkz. yukarıdaki logo cache notu).)*

*(21 Ağustos 2026, altıncı güncelleme — **Sıralama (leaderboard) özelliği eklendi.** XPBar sadece kullanıcının kendi XP/seviyesini gösteriyordu; kullanıcı rakip karşılaştırması istedi. Backend'e `GET /stats/leaderboard?period=all|weekly|monthly` (`backend/app/services/leaderboard_service.py`, `stats.py`'a bağlandı) eklendi — genel sıralama `profiles.total_xp`'den, haftalık/aylık sıralama zaten var olan `xp_events` tablosundaki zaman damgalarından hesaplanıyor (yeni migration gerekmedi). Frontend'de `web/src/components/layout/Leaderboard.tsx` oluşturulup Dashboard'a (XPBar'ın altına) bağlandı, 9 dilin hepsi için yerel çeviri eklendi. **Yolda bulunan bug:** `leaderboard_service.py` ilk halinde `zoneinfo.ZoneInfo("Europe/Istanbul")` kullanıyordu — Windows'ta `tzdata` paketi kurulu değilse bu modül import edilirken patlıyor ve `stats.py`'daki TÜM route'ları (XPBar'ın `/stats/xp`'si dahil) çökertiyor; ekranda hem Leaderboard'un sonsuz "Yükleniyor…" da kalması hem de XPBar'ın hiç görünmemesi bu yüzdendi. Türkiye 2016'dan beri yaz saati uygulamadığı için (yıl boyu sabit UTC+3) sabit `timezone(timedelta(hours=3))` offset'ine geçilip tzdata bağımlılığı tamamen kaldırıldı — kullanıcı backend'i yeniden başlatınca düzeldiği doğrulandı. Ardından kullanıcı isteğiyle Dashboard'daki `limit` 10'dan 5'e düşürüldü: artık her sekmede (Genel/Haftalık/Aylık) ilk 5 gösteriliyor, kullanıcı ilk 5'te değilse (örn. 10. sırada) 6-9 arası atlanıp sadece kendi satırı ayraçla ayrıca ekleniyor. **Ayrıca bu oturumda kullanıcıdan yeni bir büyük özellik isteği geldi — aşağıda "Madde 6 — Sosyal özellikler" olarak eklendi, henüz hiç başlanmadı.**)*

---

## ✅ Son oturumlarda tamamlananlar

- [x] **Madde 1d — Admin panelinin kapsamlı yönetim platformuna dönüştürülmesi.** Sistem sağlığı, detaylı istatistikler, iyzico ödeme takibi, kullanıcı yönetimi genişletmesi, içerik yönetimi (kelime havuzu), sosyal medya otomasyon durumu, bildirim/e-posta logları, oyun analitiği, admin işlem geçmişi (audit log), mobil uygulama görünürlük iskeleti ve rol bazlı yetkilendirme (RBAC) — hepsi bitti.
- [x] `general_word_pool` tablosu **8/8 dilde tam**: de/fr/es/it için tam (303/303) idi; MyMemory API kotası açılınca ru/ar/ja da tamamlandı (ru: 262 yeni kelime eklendi; ar ve ja zaten neredeyse tamdı — 287/303 ve 255/303 — kalan boşluklar için kaynakta karşılık bulunamadı, script idempotent şekilde atladı).
- [x] `i18n.tsx`'te Japonca (ja) arayüz dili desteği geri eklendi (9 dilin hepsi tam, 256 anahtar).
- [x] Premium/checkout sayfası tamamen `useLocale()`'e bağlandı — artık 9 dilde de gerçek çeviri kullanıyor (önceden hardcoded TR/EN idi).
- [x] `merge-preview.txt` repodan temizlendi.
- [x] `email-validator` bağımlılığı `requirements.txt`'e eklendi (`.test` TLD login hatasının kökeniydi).
- [x] `dictionary_service.py`'de gerçek bir prod bug'ı düzeltildi: Cambridge/free_dictionary'den boş `meaning_native` dönen anlamlar artık sonraki sağlayıcıya düşüyor (önceden yanlışlıkla "başarılı" sayılıyordu — `ru` gibi Cambridge'de karşılığı olmayan diller için kritikti).
- [x] **Döviz bazlı (TRY/USD/EUR) Premium fiyatlandırma altyapısı** kuruldu: backend (`config.py`, `schemas/subscription.py`, `api/routes/subscription.py`), `subscriptions.currency` kolonu (migration canlıya uygulandı), frontend (`types/index.ts`, `api.ts`, Premium sayfasında para birimi seçici). `.env`'de sadece TRY dolu olduğu için şu an davranış değişmedi — USD/EUR aşağıda "bekleyen" listesinde.
- [x] `xp_service.py` — `XP_AMOUNTS["quiz"]` teknik borç maddesi çözüldü: kod taraması "quiz" kaynağının hiçbir yerden çağrılmadığını gösterdi (oyun modları ayrılmadan önceki kalıntıydı), kaldırıldı.
- [x] `seed_schedule.py` / `scheduleTemplates.ts` — `activity_key` alanı eksikliği düzeltildi: artık program maddeleri `learning_resources` tablosuyla dile göre dinamik eşleşiyor (önceden kullanıcı öğrenilen dilini değiştirse bile program linkleri eski dilde sabit kalıyordu).
- [x] **`npm audit fix`** çalıştırıldı — 0 açık kaldı (axios 1.19.0, form-data 4.0.6, brace-expansion 5.0.9/1.1.18, js-yaml 4.3.1, @babel/core 7.29.7). `package.json` değişmedi, hepsi mevcut semver aralığında düzeldi (force gerekmedi).
- [x] **XPBar ön yüz bileşeni** — `web/src/components/layout/XPBar.tsx` oluşturuldu. Backend'de zaten hazır bekleyen `GET /stats/xp` ve frontend'de zaten tanımlı `statsApi.getXp()` / `XpSummary` kullanıyor. Sidebar'a (kompakt çubuk) ve Dashboard'a (tam kart) bağlandı, 9 dilin hepsi için yerel çeviri eklendi. **Kalan (küçük, isteğe bağlı):** Profile sayfasına da eklenebilir; seviye atlama anında toast/animasyon yok.
- [x] **Kurumsal logo seçimi ve web entegrasyonu.** 4 aday incelendi, önce 18.13.05 seçilmişti ama **kullanıcı geri bildirimiyle 18.10.47'ye (maskot karakter — gözlüklü, kitap okuyan "L") değiştirildi.** İşlenip (arka plan temizlenmiş ikon + tam logo varyantları) `web/public/logo-icon.png` + `logo-full.png` olarak eklendi; `Sidebar.tsx` (hem masaüstü hem mobil üst çubuk), `(auth)/layout.tsx` (login/register/vb. sol marka paneli) ve `login/page.tsx`'in kendi kart başlığına bağlandı — böylece logo hem masaüstünde hem mobilde (sol panel gizliyken) görünüyor. `web/src/app/favicon.ico`, `icon.png`, `apple-icon.png` de güncellendi (Next.js App Router otomatik algılama kuralı, `layout.tsx`'te ekstra kod gerekmedi). Bileşenler dosyayı sabit yoldan (`/logo-icon.png` vb.) okuduğu için kod tarafında değişiklik gerekmedi, sadece PNG/ICO dosyaları 18.10.47'den yeniden üretilip aynı yollara yazıldı.
- [x] **Mobil ve kurumsal site için marka varlıkları hazırlandı (18.10.47).** İkisi de henüz inşa edilmediği için (mobil: sadece boş `assets/`+`src/` klasör iskeleti; kurumsal site: hiç yok) kod entegrasyonu yapılamadı — ama `mobile/assets/` (logo-icon.png, icon.png, logo-full.png) ve `docs/brand/` (üç boyut/varyant) altına hazır dosyalar konuldu, bu projeler kurulduğunda doğrudan kullanılabilir.
- [x] **Logo cache sorunu çözüldü.** Web'de eski logo görünmeye devam etmesinin sebebi dosyaların yanlış olması değildi (diskte doğru dosyalar vardı) — Next.js dev server'ın `next/image` optimizasyon cache'iydi. `.next` klasörü silinip dev server yeniden başlatılınca düzeldi.
- [x] **iyzico 401 hatasında gövde-serileştirme düzeltmesi uygulandı (ama tek başına yeterli olmadı — bkz. aşağıda "devam eden").** Kullanıcı `python setup_iyzico_plans.py`'ı çalıştırınca ilk çağrıda (`create_product`) iyzico'dan `401 Unauthorized` döndü. İlk teşhis: `iyzico_client.py`'deki `_post` metodu, IYZWSv2 imzasını sıkı (boşluksuz ayraçlı, `ensure_ascii=False`) bir JSON string üzerinden hesaplıyordu, ama isteği göndermek için httpx'in `json=` parametresini kullanıyordu — o da kendi içinde Python'un *varsayılan* `json.dumps` ayarlarıyla (boşluklu ayraçlar, `ensure_ascii=True`) yeniden serileştiriyor, gönderilen baytlar imzalananla eşleşmiyordu. Bu düzeltildi ama **kullanıcı tekrar çalıştırınca 401 devam etti** — demek ki tek sebep bu değilmiş. `iyzico_client.py`/`setup_iyzico_plans.py` artık hata durumunda iyzico'nun tam hata gövdesini (`errorCode`/`errorMessage`) yazdırıyor, kesin teşhis için kullanıcının script'i tekrar çalıştırıp çıktıyı paylaşması bekleniyor.
- [x] **tsc/build hataları düzeltildi** (bir önceki güncellemede bulunmuştu): `Sidebar.tsx`/`game/page.tsx`/`forgot-password`/`reset-password`/`verify-otp` sayfalarındaki yerel sözlüklere `ja` eklendi; `words/page.tsx`'teki geçersiz çeviri anahtarı düz placeholder metne çevrildi; `register/page.tsx`'teki tanımsız `guestLang` → `locale`'e bağlandı ve `learning_lang`/`learning_langs` tip uyuşmazlığı giderildi; `schedule/page.tsx`'teki en ciddi sorun (`link()` helper'ı, `DAYS` sabiti ve `buildTemplates`/`user` tanımsızlığı) düzeltildi — `TemplateModal` artık gerçekten kendi `templates` prop'unu kullanıyor (önceden prop tanımlıydı ama hiç okunmuyordu, hep eski sabit listeye bakıyordu). `npx tsc --noEmit` artık 0 hata veriyor.
- [x] **Sıralama (leaderboard) özelliği.** Backend: `GET /stats/leaderboard?period=all|weekly|monthly` (`leaderboard_service.py`) — genel sıralama `profiles.total_xp`'den, haftalık/aylık `xp_events` tablosundaki zaman damgalarından hesaplanıyor. Frontend: `Leaderboard.tsx`, Dashboard'da XPBar'ın altında, 9 dil desteğiyle. Her sekmede ilk 5 + (varsa) kullanıcının kendi satırı ayraçla gösteriliyor, aradaki sıralar atlanıyor. Yolda çıkan `zoneinfo`/`tzdata` importu Windows'ta tüm `/stats/*`'i çökertiyordu, sabit UTC+3 offset'e geçilerek düzeltildi ve doğrulandı.
- [x] **Madde 6, Faz 1 — Arkadaşlık sistemi + Takip etme + Başkasının profilini görüntüleme.** Migration `015_social_friends.sql` (`friendships` + `follows` tabloları, RLS: herkes sadece kendi ilişkili satırlarını okuyabiliyor, yazma backend service-role client üzerinden) Supabase MCP ile canlıya uygulandı. Backend: `/api/v1/social/*` — kullanıcı arama, istek gönder/kabul et/reddet/çıkar, takip et/takipten çık, takipçi/takip edilen listesi, `GET /social/profile/{username}` (herkese açık — kullanıcı kararıyla, ek izin kontrolü yok). Frontend: `friends/page.tsx` (3 sekme) + `u/[username]/page.tsx` (profil: istatistik + çalışma programı + ilişki aksiyonları), Sidebar'a "Arkadaşlar" linki, 9 dilin hepsi için yerel çeviri. Faz bölünmesi (Faz 1/2/3) ve gizlilik kararı (herkese açık) kullanıcı onayıyla uygulandı — ayrıntı için yukarıdaki 7. güncelleme notuna bakın.
- [x] **Madde 6, Faz 2 + Faz 3 — Mesajlaşma + Engelleme + Meydan okuma (TAMAMLANDI, tek oturumda birlikte).** Migration `016_social_messaging_and_challenges.sql` (`blocks`, `conversations`, `messages`, `challenges` tabloları) canlıya uygulandı. Backend: `block_service.py`/`messaging_service.py`/`challenge_service.py` + mevcut Faz 1 servislerine engelleme entegrasyonu. Mesajlaşma polling tabanlı (Realtime yok, bilinçli tercih); meydan okuma mevcut `game_sessions`/oyun akışını yeniden kullanıyor (yeni oyun mekaniği yazılmadı). Frontend: `friends/page.tsx`'te 4. sekme, `u/[username]/page.tsx`'te Mesaj/Engelle/Meydan oku, `profile/page.tsx`'te Engellenenler listesi, yeni `messages/` sayfaları, Dashboard'da gelen kutusu önizlemesi + rozet/ikon. Ayrıntı için yukarıdaki 8. güncelleme notuna bakın. **Madde 6 artık uçtan uca tamamlandı.**
- [x] **eslint: 27 dosyada 52 hata → 0 hata, 0 uyarı; `next build` artık geçiyor (30 route).** Ayrıntı için yukarıdaki 9. (en güncel) güncelleme notuna bakın.

---

## 🔜 Devam eden / hızlı kapanabilecek maddeler

- [x] ~~**`general_word_pool` ru/ar/ja backfill.**~~ Kota açılınca kullanıcı tarafından çalıştırıldı — ru: 262 eklendi, ar: 0 eklendi (287 zaten vardı), ja: 0 eklendi (255 zaten vardı). `general_word_pool` artık 8/8 dilde tam/neredeyse tam.
- [ ] **Döviz bazlı Premium — iyzico tarafı: 401 hatası devam ediyor, kök sebep hâlâ belirsiz.** İki düzeltme denemesi (gövde serileştirme) sorunu çözmedi. `.env`'deki `IYZICO_API_KEY`/`IYZICO_SECRET_KEY` formatı kontrol edildi, temiz görünüyor (sandbox öneki yok, boşluk/tırnak sorunu yok) — yani muhtemel sebepler ya (a) key/secret çifti bu ortam (production `api.iyzipay.com`) için geçerli değil, ya da (b) iyzico merchant hesabında **Abonelik (Subscription) modülü** henüz aktif değil (yeni hesaplarda sık görülen, iyzico destek hattından etkinleştirilmesi gereken bir durum — koddan çözülemez). Script artık iyzico'nun tam hata gövdesini yazdırıyor; **bir sonraki adım kullanıcının script'i tekrar çalıştırıp "Yanıt gövdesi:" satırını paylaşması.** Çalıştırmak için:
  ```
  cd backend
  venv\Scripts\activate
  python setup_iyzico_plans.py
  ```
  **ÖNEMLİ — CANLI ORTAM UYARISI:** `backend/.env`'deki `IYZICO_BASE_URL` şu an `https://api.iyzipay.com` (sandbox DEĞİL). Script çalıştırılınca gerçek bir "Lexis Premium" ürünü ve 6 gerçek fiyatlandırma planı iyzico hesabınızda oluşur. Script çalıştırmadan önce `.env`'deki fiyatları (`PREMIUM_MONTHLY_PRICE` / `_YEARLY` / `_USD` / `_EUR`) kontrol etmenizi, çalışırken sorduğu onayı (evet/hayır) dikkatle okumanızı öneririm. Başarılı olursa 6 ref kodunu otomatik `.env`'e yazar; backend'i yeniden başlatmayı unutmayın. **Not:** Önceki denemelerde `create_product` başarısız olduysa iyzico tarafında yarım kalmış bir ürün oluşmuş olabilir — script tekrar çalıştırılmadan önce iyzico panelinden kontrol edilmesi iyi olur; script idempotent değildir, aynı isimle ikinci bir "Lexis Premium" ürünü daha oluşturabilir.
- [ ] **Kurumsal logo — kurumsal siteye/mobil uygulamaya bağlama.** Web tarafı bitti (yukarıda, 18.10.47 ile). Varlıklar `docs/brand/` ve `mobile/assets/` altında hazır; asıl bağlama işi o projeler (Madde 5 landing sayfası, React Native uygulaması) inşa edildiğinde yapılabilir.

---

## Madde 5 — Kurumsal tanıtım web sayfası

- [x] ~~Tanıtım/landing sayfası~~ — `landing/` klasöründe kuruldu (Header/Hero/Özellikler/Görüntüler/Nasıl Çalışır/SSS/CTA/Footer), sosyal medya hesabı linkleri hazır olanlar (Telegram/Slack) için ortam değişkeninden okunuyor, olmayanlar (YouTube/Instagram/X) "yakında" placeholder. Bkz. 10. güncelleme notu.
- [x] ~~Kurumsal görünüm + `/login` yönlendirmesi~~ — `web/`deki marka renkleri/font kullanıldı, Giriş/Kayıt butonları `NEXT_PUBLIC_APP_URL` üzerinden yönleniyor.
- [x] ~~Çok dilli tanıtım sayfası~~ — baştan 9 dilin hepsi (tr/en/de/fr/es/it/ru/ar/ja), `ar` için RTL doğrulandı.
- [x] ~~Logo seçimi~~ — 18.10.47 (maskot karakter) seçildi, `docs/brand/` altında hazır varlıklar var; `landing/public/`e de kopyalandı.
- [x] ~~Gerçek ekran görüntülerini alma~~ — Dashboard/Kelimeler/Kelime Oyunu'ndan gerçek görüntüler alındı, sıralamadaki test hesap adları kırpılarak dışarıda bırakıldı, `landing/`e entegre edildi ve doğrulandı. Bkz. 11. güncelleme notu.
- [x] ~~Vercel'e deploy~~ — kullanıcı GitHub (`abtaylan/lexis`) üzerinden Vercel'e import edip canlıya aldı, sonucu ekran görüntüsüyle doğruladı. Şu an geçici `*.vercel.app` adresinde.
- [x] ~~LinkedIn'in sosyal medya listesine eklenmesi~~ — `config.ts`/`icons.tsx`/`Footer.tsx` güncellendi, hesap henüz açılmadığı için diğerleri gibi "yakında" placeholder. Bkz. 12. güncelleme notu.
- [ ] **Her dil için ayrı ekran görüntüsü** — şu an 9 dilin hepsinde aynı Türkçe içerikli ekran görüntüleri gösteriliyor, dil değiştirildiğinde görsel içerik değişmiyor. Kullanıcı tarafından fark edildi (12. güncelleme notu), düzeltilmesi gerekiyor — en azından İngilizce için ayrı bir set (ya da idealde 9 dilin hepsi için) gerçek ekran görüntüsü alınmalı.
- [ ] **Mobil uygulama linkleri/karekod** — App Store, Google Play rozetleri + QR kod + erişim linkleri kurumsal siteye eklenmeli. **Güncellendi (13. güncelleme):** mobil uygulamanın Faz 1'i artık çalışıyor ve Android APK olarak test edildi, ama henüz Play Store'da/App Store'da yayında değil — gerçek mağaza linki hâlâ yok. Karar bekleniyor: mağaza yayınına kadar beklenecek mi, yoksa geçici olarak APK'nın doğrudan indirme linki mi konacak?
- [ ] **İletişim/sosyal bilgilerinin netleştirilmesi ve Vercel'e girilmesi** — gerçek iletişim e-postası, Telegram/Slack/LinkedIn linkleri, production alan adı (`NEXT_PUBLIC_APP_URL`) hem `.env.local`'e hem Vercel proje ayarlarına girilmeli (bkz. `landing/README.md`). **Kullanıcı kararıyla ertelendi** — mobil uygulama ve domain alımı ile birlikte, canlıya geçiş tamamlanınca yapılacak.
- [ ] **Kendi domain'in bağlanması** — kullanıcı kararıyla ertelendi, mobil uygulama tamamlanınca birlikte yapılacak.

## ~~Yeni bulunan — eslint taraması~~ — ✅ ÇÖZÜLDÜ

- [x] ~~**eslint: 27 dosyada 52 hata — `next build` başarısız oluyordu.**~~ Ayrıntı için yukarıdaki 9. (en güncel) güncelleme notuna bakın. `npx eslint src` → 0 hata/0 uyarı, `npx tsc --noEmit` → 0 hata, `npx next build` → başarılı.

## Production'a geçiş öncesi bekleyen maddeler

- [ ] iyzico callback entegrasyonunun canlı ortamda doğrulanması (plan referansları girildikten sonra gerçek bir abonelik akışı uçtan uca test edilmeli)
- [ ] Production OTP modu: `OTP_MODE=real` + Gmail App Password ile gerçek e-posta gönderimi
- [ ] `backend/expire_premium.py` / `backend/send_schedule_reminders.py` cron kurulumu
- [x] ~~`next build`'in gerçekten geçtiğinin doğrulanması~~ — eslint temizliğiyle birlikte doğrulandı, 30 route başarıyla üretiliyor (bkz. 9. güncelleme notu).

## Büyük, henüz başlanmamış işler

- [x] ~~XPBar ön yüz bileşeni~~ — yapıldı, yukarıya taşındı.
- [x] ~~Sıralama (leaderboard)~~ — yapıldı, yukarıya taşındı.
- [x] ~~**React Native mobil uygulama — Faz 1**~~ — Expo/TypeScript ile sıfırdan inşa edildi (auth+OTP, dashboard, kelimeler, oyun, program, profil, push bildirim altyapısı), backend'e `push_tokens` eklendi, Android APK üretilip tablette test edildi. Ayrıntı için yukarıdaki 13. (en güncel) güncelleme notuna bakın.
- [ ] **Mobil Faz 2** — sosyal özellikler (arkadaşlık/mesajlaşma/meydan okuma) mobile taşınması + ilgili push bildirimleri.
- [ ] **Mobil Faz 3** — Premium/WebView satın alma akışı, bildirim tercihleri ekranı.
- [ ] **Mobil Faz 4** — mağaza yayın hazırlığı: ikon çözünürlükleri/Android monochrome-background görselleri, `eas.json` production profili, App Store/Google Play görselleri ve açıklama metinleri, iOS için Apple Developer Program üyeliği. **Yayınlandıktan sonra unutulmaması gereken geri bağlantı:** App Store/Google Play linkleri + karekod, kurumsal `landing/` sitesine de eklenmeli (bkz. Madde 5).
- [ ] Ek oyun modları — `game_mode` enum'ında `typing`/`matching`/`listening`/`sprint` tanımlı ama sadece `wordle` ve `multiple_choice` gerçekten implemente edilmiş
- [ ] `backend/tests/` — otomatik test altyapısı

## Madde 6 — Sosyal özellikler (arkadaşlık / takip / mesajlaşma / meydan okuma) — ✅ TAMAMLANDI

*(21 Ağustos 2026'da kullanıcı tarafından eklendi. Kapsam 3 faza bölündü (asistan önerisi, kullanıcı onayladı): Faz 1 = arkadaşlık+takip+profil görüntüleme, Faz 2 = mesajlaşma+engelleme, Faz 3 = meydan okuma. **Üç fazın hepsi de bitti** — Faz 1 ayrı bir oturumda (bkz. 7. güncelleme notu), Faz 2 ve Faz 3 kullanıcının açık isteğiyle ("İkisini birlikte tek oturumda yap") tek oturumda birlikte (bkz. 8./en güncel güncelleme notu).)*

### Faz 1 — bitti

- [x] ~~Arkadaşlık sistemi~~ — istek gönder/kabul et/reddet/çıkar, arkadaş listesi. `friendships` tablosu (canlıya uygulandı).
- [x] ~~Takip etme~~ — arkadaşlıktan bağımsız, tek yönlü. `follows` tablosu (canlıya uygulandı).
- [x] ~~Başkasının profilini görüntüleme~~ — **kullanıcı kararıyla herkese açık** (giriş yapmış her kullanıcı görebilir, ek izin kontrolü yok, engellenen taraf hariç — bkz. Faz 2). İstatistik özeti + çalışma programı `GET /social/profile/{username}` altında.

### Faz 2 — mesajlaşma + engelleme — bitti

- [x] ~~**Mesajlaşma (DM)**~~ — birebir sohbet. `conversations`/`messages` tabloları (canlıya uygulandı). **Netleşen karar:** gerçek zamanlı iletim yerine polling (bu kod tabanında hiç Realtime emsali yoktu, tutarlılık için polling seçildi — gelen kutusu 8sn, açık sohbet 4sn'de bir tazeleniyor). Okunmadı sayacı var (`GET /social/messages/unread-count`, Dashboard'da rozet), bildirimler mevcut `notifications` altyapısına entegre edildi.
- [x] ~~**Engelleme (block)**~~ — `blocks` tablosu (canlıya uygulandı). Engellenen kullanıcı mesaj gönderemez, profil/istatistik göremez, arkadaşlık/takip isteği atamaz (karşılıklı kontrol edilir, `is_blocked_either_way`); engelleme aradaki arkadaşlığı/takibi de otomatik temizler. Yönetimi `profile/page.tsx`'teki "Engellenenler" bölümünden. Report/şikayet akışı eklenmedi (opsiyonel, ileride admin audit log'a bağlanabilir).

### Faz 3 — meydan okuma — bitti

- [x] ~~**Meydan okuma (challenge)**~~ — `challenges` tablosu (canlıya uygulandı). Sadece arkadaşlar arasında yapılabiliyor. Yeni bir oyun mekaniği YAZILMADI — mevcut `game_sessions` akışı olduğu gibi kullanılıyor: davet kabul edilince her taraf oyunu normal şekilde oynuyor, bitmiş oturumunu `submit-score` ile gönderiyor, her iki taraf da gönderince backend skorları karşılaştırıp kazananı belirliyor ve bildirim gönderiyor. Şu an sadece `multiple_choice` ve `wordle` modları seçilebiliyor (diğer `game_mode` değerleri enum'da var ama oyun sayfasında hiç implemente edilmemiş, bkz. "Ek oyun modları" maddesi).

## En son yapılacaklar — tüm test geliştirmeleri sonlanıp mobil ve web erişim hizmetleri aktif olduğunda

### Madde 3b: Sosyal medya içerik paylaşımı

- [x] Telegram bot + kanal, Slack workspace + webhook kurulumu
- [x] Backend otomasyonu (`post_daily_content.py`, dry-run modu)
- [ ] Dry-run testinin oluşturduğu test `social_posts` kaydının temizlenmesi (istenirse)
- [ ] Gerçek canlı gönderim testi (`SOCIAL_POST_MODE=real`) — açık onay bekleniyor
- [ ] VPS'e cron kurulumu — production'a alma

### Diğer platform hesapları (kullanıcı kendi yapacak)

*(21 Ağustos 2026, 12. güncelleme ile güncellendi: bu listedeki eski WhatsApp Business/Facebook referansı, kurumsal site (Madde 5) için kullanıcının netleştirdiği nihai listeyle güncellendi — WhatsApp kullanıcı kararıyla dahil değil, Facebook listede yok, LinkedIn eklendi.)*

- [x] Hesap açıklaması (bio) metinleri + profil resmi hazırlandı
- [x] Telegram, Slack — zaten kurulu (bkz. Madde 3b)
- [ ] YouTube, Instagram, X, LinkedIn hesapları
- [ ] Hesaplar açıldıkça kullanıcı adları/URL'leri paylaşılıp `landing/`deki `.env.local`'e ve footer'a eklenmesi

---

**Durum özeti:** Admin panel (Madde 1d), i18n/Premium çeviri işleri, npm audit ve tsc derleme hataları tamamen bitti; **general_word_pool artık 8/8 dilde tam/neredeyse tam** (ru/ar/ja backfill kota açılınca çalıştırıldı); **logo 18.10.47'ye (maskot) değiştirildi**, web'e bağlandı ve görüntüleme cache sorunu da çözüldü; döviz bazlı Premium'un kod tarafı bitti ama iyzico plan oluşturma script'i **hâlâ 401 Unauthorized veriyor** — iki düzeltme denemesine rağmen kök sebep belirlenemedi, script artık iyzico'nun tam hata mesajını gösteriyor, kullanıcının tekrar çalıştırıp o mesajı paylaşması bekleniyor; XPBar ve **Sıralama (leaderboard)** bileşenleri Dashboard'a bağlandı; **eslint artık tamamen temiz** (web); **Madde 6 (sosyal özellikler, web) uçtan uca TAMAMLANDI** — Faz 1/2/3 hepsi bitti; **Madde 5 (kurumsal tanıtım sitesi) Vercel'de canlıda**, kalan tek şey ekran görüntülerinin dil bazlı çeşitlenmesi + iletişim/domain bilgilerinin netleşip girilmesi; **ve şimdi React Native mobil uygulamanın Faz 1'i de bitti** — Expo tabanlı uygulama çalışıyor, Android APK üretilip gerçek bir tablete kurulup doğrulandı (bkz. 13. güncelleme notu); iOS tarafı Apple Developer Program üyeliği bekliyor, mobil Faz 2/3/4 (sosyal özellikler, Premium, mağaza yayını) henüz başlanmadı.

**Kullanıcı ile üzerinde anlaşılan sıra:** (1) acil/öncelikli işler [eslint ✅ bitti; iyzico 401 hâlâ bekliyor] → (2) Madde 5, kurumsal tanıtım web sayfası [**iskelet + içerik + 9 dil + gerçek ekran görüntüleri + Vercel'e canlı deploy ✅ bitti**; kalanlar: dil bazlı ayrı ekran görüntüleri, mobil uygulama linkleri/karekod, iletişim/sosyal bilgilerinin netleşip Vercel'e girilmesi ve domain bağlanması] → (3) React Native mobil uygulama [**Faz 1 ✅ bitti, Android APK doğrulandı**; kalanlar: iOS/Apple Developer hesabı, Faz 2/3/4] → (4) sosyal medya hesapları (Madde 3b + diğer platform hesapları — YouTube/Instagram/X/LinkedIn, kullanıcı kendi açacak, hesaplar açılınca env/Vercel adımları hazırlanacak). **Sıradaki adım: kullanıcı kararına bağlı — mobil Faz 2'ye mi devam edilecek, yoksa Madde 5'e (site görselleri/env) mi dönülecek, yoksa sosyal medya hesapları mı önce açılacak.**
